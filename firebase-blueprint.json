{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "Represents a sovereign user reputation passport profile and dynamic hold history indicators.",
      "type": "object",
      "properties": {
        "address": {
          "type": "string",
          "description": "The unique 42-character EVM hex address identifier."
        },
        "username": {
          "type": "string",
          "description": "The alphanumeric handle chosen by the sovereign user."
        },
        "hideWallet": {
          "type": "boolean",
          "description": "Indicates if the wallet address should be obscured on the leaderboard."
        },
        "streak": {
          "type": "integer",
          "description": "Day countdown of continuous holding behavior."
        },
        "connectedAt": {
          "type": "string",
          "format": "date-time",
          "description": "The ISO timestamp when the passport was compiled."
        },
        "karmaScore": {
          "type": "integer",
          "description": "Web3 aggregate reputation and reliability score (0-1000 range)."
        },
        "personality": {
          "type": "string",
          "description": "Reputation personality archetype."
        },
        "auraPoints": {
          "type": "integer",
          "description": "Reward aura balance details."
        },
        "lastClaimedAt": {
          "type": "string",
          "description": "ISO timestamp for when the last daily airdrop was claimed."
        }
      },
      "required": ["address", "username", "karmaScore"]
    },
    "AuthChallenge": {
      "title": "AuthChallenge",
      "description": "Transient cryptographic challenge message details mapped to the user address.",
      "type": "object",
      "properties": {
        "address": {
          "type": "string",
          "description": "Target signing address."
        },
        "challenge": {
          "type": "string",
          "description": "Unique cryptographic handshake UUID value."
        },
        "createdAt": {
          "type": "integer",
          "description": "EVM signature session generation coordinate timestamp."
        }
      },
      "required": ["address", "challenge", "createdAt"]
    },
    "SyncSession": {
      "title": "SyncSession",
      "description": "Active handshaking session for connecting external desktop/mobile browser logins.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "The unique sync session ID."
        },
        "status": {
          "type": "string",
          "description": "The current verification stage."
        },
        "username": {
          "type": "string",
          "description": "Username assigned by user on originator screen."
        },
        "hideWallet": {
          "type": "boolean",
          "description": "Psuedonymity override configurations."
        },
        "challenge": {
          "type": "string",
          "description": "Secure UUID challenge generated by server."
        },
        "message": {
          "type": "string",
          "description": "Poetic personal_sign challenge prompt."
        },
        "address": {
          "type": "string",
          "description": "Sovereign address of verified wallet."
        },
        "signature": {
          "type": "string",
          "description": "Validated signature payload."
        },
        "createdAt": {
          "type": "integer",
          "description": "Session genesis milliseconds."
        }
      },
      "required": ["id", "status", "challenge", "message", "createdAt"]
    }
  },
  "firestore": {
    "/profiles/{address}": {
      "schema": "UserProfile",
      "description": "Individual reputation coordinates mapped by public key address."
    },
    "/challenges/{address}": {
      "schema": "AuthChallenge",
      "description": "Transient validation message structures for cryptographic verify flows."
    },
    "/sync_sessions/{id}": {
      "schema": "SyncSession",
      "description": "Shared external browser cryptographic handshake credentials."
    }
  }
}
